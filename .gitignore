node_modules
dist
dist-ssr
.vite

*.local
*.tsbuildinfo

.DS_Store
Thumbs.db

.idea
.vscode/*
!.vscode/extensions.json

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Word lock files
~$*

# Chờ cấp scope `workflow` cho token gh mới đẩy được file này lên.
# Chạy `gh auth refresh -s workflow` rồi xoá dòng dưới đây.
.github/workflows/deploy.yml

# Tài liệu kinh doanh nội bộ.
# Repo này đang ở chế độ public, nên báo cáo pitch và tài liệu nghiên cứu
# chiến lược không được đưa lên. Nếu muốn công khai, xoá hai dòng dưới đây.
VHuman-BaoCao.docx
deep-research-report.md
VHuman_TuVi_Career_Classification_MVP.md
